// packages like lazysizes run on their own without any additional code so good practice to place it in a separate file. Then update webpack config so can build a final script file
import "lazysizes";
